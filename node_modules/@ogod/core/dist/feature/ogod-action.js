import customAttributes from "../../../../custom-attributes/index.js";
import { CustomAttribute, ogodStore } from "../core/index.js";
export class OgodActionAttribute extends CustomAttribute {
  connectedCallback() {
    this.action = JSON.parse(this.value);
    this.ownerElement.addEventListener('click', this.fireAction.bind(this));
  }

  disconnectedCallback() {
    this.ownerElement.removeEventListener('click', this.fireAction.bind(this));
  }

  changedCallback() {
    this.action = JSON.parse(this.value);
  }

  fireAction() {
    ogodStore.dispatch(this.action);
  }

}
customAttributes.define('ogod-action', OgodActionAttribute); //# sourceMappingURL=ogod-action.js.map