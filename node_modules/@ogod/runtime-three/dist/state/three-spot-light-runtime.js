import { OgodInstanceEntity, loadedInstance, DefaultInstanceRuntime } from "../../../core/dist/index.js";
import { of } from "../../../../rxjs/_esm5/index.js";
import { SpotLight } from "../../../../three/build/three.module.js";
export class ThreeSpotLightEntity extends OgodInstanceEntity {}
export class ThreeSpotLightRuntime extends DefaultInstanceRuntime {
  load(entity) {
    this.instance = new SpotLight(entity.color, entity.intensity, entity.distance, entity.exponent, entity.decay);
    return of(loadedInstance(entity));
  }

  initialize(entity) {
    let e = super.initialize(entity);

    if (entity.castShadow) {
      this.instance.castShadow = true;
    }

    if (entity.properties.POSITION) {
      let pos = entity.properties.POSITION.entity.glValue;
      this.instance.position.set(pos.x, pos.y, pos.z);
    }

    return e;
  }

  destroy() {}

} //# sourceMappingURL=three-spot-light-runtime.js.map